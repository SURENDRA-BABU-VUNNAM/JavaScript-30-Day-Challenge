// Task 7: Select an HTML element and change one of its attributes (e.g., src of an img tag).

let button = document.querySelector("#orange");
// button.id = "new_id";

button.setAttribute("id", "new_id");
